apiVersion: apps/v1
kind: Deployment
metadata:
  name: storage
spec:
  replicas: 1
  selector:
    matchLabels:
      app: MiniIO
template:
    metadata:
      labels:
        app: MiniIO
    spec:
      containers:
      - name: storage
        image: busybox
        command: ["sh","-c"]
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: storage-svc
spec:
  selector:
    app: MiniIO
  ports:
  - port: 80
    targetPort: 8080
